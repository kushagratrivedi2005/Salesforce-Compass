# Vehicle Sales Forecasting API Requirements

# Flask web framework and extensions
Flask==3.0.0
flask-cors==4.0.0

# Data manipulation and analysis
pandas>=2.2.0
numpy>=1.26.0

# Machine learning and statistics (existing dependencies)
statsmodels>=0.14.0
scikit-learn>=1.7.0
# pmdarima==2.0.4  # Currently has build issues on Windows with Python 3.13
# To install pmdarima later, try: pip install pmdarima --only-binary=all
# Or use alternative ARIMA implementations from statsmodels

# Visualization (existing dependencies)
matplotlib>=3.10.0

# Additional utilities
python-dateutil==2.8.2
pytz==2023.3
setuptools>=65.0.0
requests

# Development and testing
pytest==7.4.3
pytest-flask==1.3.0

# Production server (optional)
gunicorn==21.2.0
